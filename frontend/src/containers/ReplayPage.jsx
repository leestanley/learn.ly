import React from 'react';
import { connect } from 'react-redux';

import ReplayPage from '../components/ReplayPage';

const ReplayPageContainer = props => {
  return (
    <ReplayPage />
  );
};

export default ReplayPageContainer;
